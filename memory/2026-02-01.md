# ToneStore Dev Log - 2026-02-01

## 작업 완료

### 페달 추가 UI 통합
- `PedalBoardSection.kt` 리팩토링
- "페달 추가" + "커스텀 페달 추가" 버튼 → "페달 추가" 하나로 통합
- 페달보드 화면의 `PresetPedalSelectionDialog` 바텀시트 재사용

### 톤 저장 에러 표시
- `CreateToneIntent.ClearError` 추가
- `CreateToneScreen`에 에러 Snackbar 표시 `LaunchedEffect` 추가
- 간헐적 저장 실패 원인: Firebase Auth 상태 로드 전 `currentUser` null

### 톤 공유 후 게시글 이동
- `ShareToneState.sharedPresetId` 추가
- `onShareSuccess(presetId)` 콜백으로 변경
- NavGraph에서 공유 완료 시 `PresetDetail` 화면으로 이동

### 페달 선택 UI 리디자인 (2025/2026 트렌드)
- `PresetPedalSelectionDialog.kt` 완전 재작성
- 3D 스큐어모픽 페달 카드:
  - 실제 페달처럼 LED, 노브, 풋스위치 시각화
  - 프레스 애니메이션 (scale + elevation)
  - 페달 색상별 그림자 효과
- 그라디언트 커스텀 페달 버튼
- 이모지 카테고리 필터 (🔥드라이브 🌊모듈레이션 등)
- `PedalCategory` enum 활용

## 커밋 이력
- `da8d6a2` - feat: 톤 생성화면 페달 추가 UI 개선 + 저장 에러 표시
- `230e591` - feat: 공유 완료 후 게시글 이동 + 페달 선택 UI 대폭 개선

## 기술 노트
- `PedalCategory` enum: DRIVE, MODULATION, TIME_BASED, DYNAMICS, UTILITY, PITCH
- `getCategoryForPedal()`: 페달 이름 → 카테고리 매핑
- `SkeuomorphicPedalCard`: 실제 페달 느낌의 3D 카드 컴포넌트
- `MiniKnob`: 노브 미리보기용 작은 노브 컴포넌트
